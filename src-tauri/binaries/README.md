# This directory holds Tectonic sidecar binaries
# Binaries are downloaded by CI and should NOT be committed to git

# Naming convention (Tauri auto-detects platform):
# tectonic-x86_64-unknown-linux-gnu      (Linux)
# tectonic-x86_64-pc-windows-msvc.exe    (Windows)
# tectonic-aarch64-apple-darwin          (macOS ARM)
# tectonic-x86_64-apple-darwin           (macOS Intel)
